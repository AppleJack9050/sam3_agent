# NVIDIA RTX 5090 (Blackwell, sm_120) needs CUDA 12.8+ and torch>=2.7.
# Install with:
#   pip install --extra-index-url https://download.pytorch.org/whl/cu128 \
#       "torch>=2.7" torchvision
torch>=2.7
torchvision
# sam3 pins numpy<2; opencv-python >= 4.11 requires numpy>=2. Pin both
# to the last versions that coexist.
numpy<2
opencv-python==4.10.0.84
Pillow
fastapi
uvicorn[standard]
pydantic>=2
python-multipart
# Orchestration LLM: Claude Agent SDK (replaces langgraph + langchain).
# Requires the Claude Code CLI on PATH (`npm i -g @anthropic-ai/claude-code`)
# and a logged-in subscription (`claude login` or `claude setup-token`).
# Keep ANTHROPIC_API_KEY UNSET so billing uses your Claude plan, not the API.
claude-agent-sdk>=0.2
# SAM 3 runtime deps not declared in the sam3 package
einops
hydra-core
pycocotools
psutil
av
decord
timm
ftfy
regex
matplotlib
scipy
# SAM 3 (install from source per facebookresearch/sam3 README)
# pip install git+https://github.com/facebookresearch/sam3.git
# Dev
pytest
httpx
# SAM 3 (install from source per facebookresearch/sam3 README)
# sam3 @ git+https://github.com/facebookresearch/sam3.git
# Optional: ultralytics integration
# ultralytics
